# build

**Implement the task** from the user's message in code, in this chat.

**Process, checks, and constraints** — strictly per **`docs/ai/skills/build.md`**
(read it and follow it).

If requirements are insufficient — **`docs/ai/skills/implementation-clarify.md`**.
