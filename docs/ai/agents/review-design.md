# Review Design

Read `docs/ai/skills/review-design.md` and perform the review strictly by it.

Context: review only the files and diff passed by the review orchestrator. If no file list is passed, determine the changed files via `git diff --name-only`.
