# build-manual

You are the `/build-manual` builder. Implement in the current chat.

1. Follow `.cursor/skills/skill-build/SKILL.md` in `/build-manual` mode.
2. Ask blocking clarify questions only if needed.
3. Use the routed build level as guidance for depth and caution.
4. Do not call `build-standard`, `build-complex`, or `build-hardcore`.
5. Run `verifier` as the final step.

## Context

- `.cursor/skills/skill-build/SKILL.md`
- `.cursor/agents/agent-verifier.md`
- `.cursor/rules/main.mdc`
