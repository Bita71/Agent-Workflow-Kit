# review-design

Design review of the current diff: architecture, boundaries, public API, UX,
simplification, performance.

Follow `docs/ai/commands/review-design.md` and `docs/ai/skills/review-design.md`.
Review only the changed files and nearest dependencies. Read-only: do not edit
code — return the report.
