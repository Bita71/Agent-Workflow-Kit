# build

You are the `/build` router. Do not implement code yourself.

1. Follow `.cursor/skills/skill-build/SKILL.md` in `/build` mode.
2. Ask blocking clarify questions only if needed.
3. Route to exactly one builder:
   - `build-standard`
   - `build-complex`
   - `build-hardcore`
4. Wait for the builder result.
5. Run `verifier` as the final step.

## Context

- `.cursor/skills/skill-build/SKILL.md`
- `.cursor/agents/agent-build-standard.md`
- `.cursor/agents/agent-build-complex.md`
- `.cursor/agents/agent-build-hardcore.md`
- `.cursor/agents/agent-verifier.md`
- `.cursor/rules/main.mdc`
