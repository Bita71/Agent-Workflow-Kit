name = "review_security"
description = "Read-only Codex security reviewer, driven by risk signals in the diff."
model = "gpt-5.5"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Read `docs/ai/agents/review-security.md` and `docs/ai/skills/review-security.md`.
Run only when the diff shows risk signals (auth, payments, user input, storage,
external APIs, secrets, dependencies). Do not edit code — return the report only.
"""
