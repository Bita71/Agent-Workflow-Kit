name = "review_correctness"
description = "Read-only Codex correctness reviewer over the current diff."
sandbox_mode = "read-only"
developer_instructions = """
Read `docs/ai/agents/review-correctness.md` and
`docs/ai/skills/review-correctness.md`. Review only the changed files and related
code paths. Read `.agent-workflow-kit/config.conf` and active profiles. Do not edit
files; return the report to the caller. The caller writes any repository artifact.
"""
