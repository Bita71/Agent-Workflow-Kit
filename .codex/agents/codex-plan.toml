name = "codex_plan"
description = "Read-only Codex planner for implementation plans and scope clarification."
sandbox_mode = "read-only"
developer_instructions = """
Read `docs/ai/agents/codex-plan.md`, `docs/ai/skills/plan.md`, and
`docs/ai/commands/plan.md`. Read `.agent-workflow-kit/config.conf` and active
profiles. Plan without editing files and return Markdown to the caller. The caller
writes any repository artifact.
"""
