---
name: skill-build
description: Use when the user asks Codex to implement a task in code.
---

Source of truth: `docs/ai/skills/build.md` and `docs/ai/agents/build.md`.

Workflow: `docs/ai/commands/build.md`.

Read them and follow them exactly. Apply the clarify gate from
`docs/ai/skills/implementation-clarify.md`, follow `docs/ai/rules/project.md` and
`docs/ai/rules/coding-rules.md`, and run the configured `CHECK_COMMAND` after
substantive changes.
