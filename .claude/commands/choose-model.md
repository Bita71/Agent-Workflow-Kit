# choose-model

Recommend which model and effort to use for the given work (often a `.plan.md`).
Do not write code or run anything — just the recommendation.

Follow `docs/ai/commands/choose-model.md`.
