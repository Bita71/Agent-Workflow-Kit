---
description: Final factual validation - changed files, exports, imports, security grep, deps, CI
---

Invoke the `verifier` subagent.

Use it at the end of every build task, after direct code edits, or when explicitly requested.
