---
description: Implement in the current chat using build-routing guidance, then verify
---

Implement in this chat.

1. Follow `.claude/skills/skill-build/SKILL.md`.
2. Ask blocking clarify questions only if needed.
3. Use the routed build level as guidance for depth.
4. Do not invoke `build-standard`, `build-complex`, or `build-hardcore` subagents.
5. Invoke the `verifier` subagent as the final step.
